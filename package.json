{
  "name": "lamed-project",
  "version": "1.0.0",
  "description": "Orquestrador de Deploy para Lamed",
  "scripts": {
    "deploy:backend": "cd backend && gcloud run deploy monfintrack-backend --source . --region southamerica-east1 --quiet",
    "deploy:frontend": "cd frontend && npm install --legacy-peer-deps && npm run build && firebase deploy --only hosting",
    "deploy": "npm run deploy:backend && npm run deploy:frontend"
  },
  "author": "",
  "license": "ISC"
}
